#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(false);

    int n;
    cin >> n;
    vector<tuple<int,int,int>> v;
    vector<int> contains(n, 0), contained(n, 0);
    for(int i = 0; i < n; i++) {
        int a, b; cin >> a >> b;
        v.emplace_back(a, 1 
    }
}
