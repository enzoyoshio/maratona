#include <bits/stdc++.h>

#define int long long

using namespace std;

const int MAXN = 5050;
int n;
vector<int> v;
int tb[MAXN][MAXN];

signed main() {
    cin.tie(0)->sync_with_stdio(false);

    cin >> n;
    v.assign(n, 0);
    for(int i = 0; i < n; i++)
        cin >> v[i];


}
