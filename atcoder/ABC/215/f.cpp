#include <bits/stdc++.h>

using namespace std;
#define int long long

signed main() {
	cin.tie(0)->sync_with_stdio(false);

	int n;
	cin >> n;
	vector<pair<int,int>> v(n);
	for(auto& [a, b]: v) cin >> a >> b;

	int ans1 = 0, ans2 = 1;

	for(int i = 2; i < n; i++) {
		
	}
}
