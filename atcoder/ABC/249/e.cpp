#include <bits/stdc++.h>

using namespace std;
#define int long long

int n, p;

int f(int idx, int len, int prev) {

	if(len >= n) return 0;

	if(idx == n) return 1;

	int ans = 0;
	5
}
signed main() {
	cin.tie(0)->sync_with_stdio(false);

	cin >> n >> p;
}
