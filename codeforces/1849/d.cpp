#include <bits/stdc++.h>

using namespace std;
#define int long long

int n;
vector<int> v;

int dp(int idx, int prev) {

	if(idx >= n) return 0;


}

signed main() {
	cin.tie(0)->sync_with_stdio(false);

	int t; cin >> t; while(t--) {
		cin >> n;
		v.resize(n);
		for(auto& it: v)
			cin >> it;
	}
}
